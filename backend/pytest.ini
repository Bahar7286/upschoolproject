[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    unit: isolated unit tests (fast)
    integration: HTTP API tests via TestClient
    full_geo: integration tests requiring 81 cities / districts seed
